# Конвенции разработки

## Перечисления

- Списки перечислений создаются используя статические методы перечислений `tryFromList()`, `tryFromNullableList`, при этом надо убедиться, что трейт EnumHelperTrait используется перечислением
